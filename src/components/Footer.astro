---
import { footerColumns } from "../data/navigation";
const year = new Date().getFullYear();
---
<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-col brand-col">
      <a class="footer-brand" href="/" aria-label="KarbonFiyat ana sayfa">
        <span class="footer-brand-mark" aria-hidden="true">
          <img
            src="/logo/karbonfiyat-logo.png"
            alt=""
            class="footer-logo-img"
            width="38"
            height="38"
            loading="lazy"
            decoding="async"
          />
        </span>
        <span class="footer-brand-text">karbonfiyat</span>
      </a>
      <p class="brand-desc">
        Karbon fiyatını finansal karara çeviren Carbon Financial Intelligence platformu.
      </p>
    </div>

    {footerColumns.map(col => (
      <div class="footer-col">
        <strong class="col-title">{col.title}</strong>
        <nav class="col-links" aria-label={col.title + " linkleri"}>
          {col.links.map(link => {
            const isExt = link.href.startsWith("http");
            return (
              <a
                href={link.href}
                target={isExt ? "_blank" : undefined}
                rel={isExt ? "noopener" : undefined}
              >
                {link.label}
              </a>
            );
          })}
        </nav>
      </div>
    ))}
  </div>

  <div class="container footer-bottom">
    <span class="sub-copy mono">© {year} KarbonFiyat</span>
    <span class="sub-notice">
      Resmî, piyasa, tahmin ve kullanıcı senaryosu verileri ayrı statülerde gösterilir.
    </span>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--border-soft);
    margin-top: 120px;
    padding: 64px 0 32px;
    background: #0A0C0F;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 1.35fr 1fr 1fr 1fr;
    gap: 36px;
  }
  .footer-col {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .footer-brand {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    color: var(--text-primary);
    font-size: 20px;
    font-weight: 750;
    letter-spacing: -0.035em;
    text-decoration: none;
  }
  .footer-brand-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 38px;
    height: 38px;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    flex-shrink: 0;
    transition: border-color 180ms ease, background 180ms ease, transform 180ms cubic-bezier(.2,.8,.2,1);
  }
  .footer-logo-img {
    height: 32px;
    width: 32px;
    display: block;
    object-fit: contain;
    filter: none;
  }
  .footer-brand-text {
    color: #F3F4F6;
    letter-spacing: -0.035em;
    font-size: 19px;
  }
  .footer-brand:hover .footer-brand-mark {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.16);
    transform: translateY(-1px);
  }
  .brand-desc {
    color: var(--text-secondary);
    font-size: 14px;
    line-height: 1.6;
    margin: 0;
    max-width: 290px;
  }
  .col-title {
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-primary);
    font-weight: 650;
  }
  .col-links {
    display: flex;
    flex-direction: column;
    gap: 9px;
  }
  .col-links a {
    color: var(--text-secondary);
    font-size: 14px;
    transition: color 150ms ease;
  }
  .col-links a:hover {
    color: var(--text-primary);
  }
  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    padding-top: 40px;
    margin-top: 48px;
    border-top: 1px solid var(--border-soft);
    font-size: 12px;
  }
  .sub-copy {
    font-size: 12px;
    color: var(--text-primary);
    font-weight: 550;
  }
  .sub-notice {
    color: var(--text-secondary);
  }

  @media (max-width: 860px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
      gap: 32px;
    }
    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
      gap: 10px;
    }
  }
  @media (max-width: 520px) {
    .footer-grid {
      grid-template-columns: 1fr;
    }
  }
</style>